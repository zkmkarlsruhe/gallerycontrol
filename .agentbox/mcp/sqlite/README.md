# SQLite MCP Server

SQLite database operations with schema inspection and query capabilities.

## Installation

```bash
abox mcp add sqlite
```

## Configuration

The default database path is `/workspace/data.db`. You can customize this in the config.

## Usage

- Read database schema
- Execute SQL queries
- Analyze data
- Create and modify tables
